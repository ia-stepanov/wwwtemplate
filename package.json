{
  "name": "wwwtemplate",
  "version": "1.0.0",
  "description": "Стартовый проект для сборки и разработки веб-макетов и UI Kit к ним.",
  "scripts": {
    "dev": "parcel src/index.html --open",
    "kit": "parcel src/uikit.html --open",
    "build:uikit": "rm -rf npm run && parcel build src/uikit.html --dist-dir build/uikit",
    "build": "rm -rf build && parcel build src/index.html --dist-dir build",
    "deploy": "gh-pages -d build"
  },
  "author": "Mikhail Nepomnyashchiy",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.4.16",
    "parcel": "^2.12.0",
    "postcss-modules": "^4.3.1",
    "sass": "^1.69.5"
  },
  "browserslist": [
    "last 5 version",
    "ie 10"
  ],
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "gh-pages": "^6.1.1"
  },
  "volta": {
    "node": "20.10.0"
  }
}
